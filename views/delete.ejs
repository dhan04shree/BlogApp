<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogApp</title>
    <style>
        body{background-color:#cfffff;}
        .showblog{
            text-align: start;
            background-color:#9de2e2;
            margin: 60px 120px;
            border:1px solid #72a2a2;
            border-radius: 8px;
            padding: 10px 20px;
        }
        .showblog p{
            padding: 30px 0 50px 0;
            font-size: 20px;
            text-align:justify;
        }
        .showblog form{
            display:inline;
        }
        .delbtn:hover{
            background-color:crimson;
        }
        .delbtn{
            border-radius: 10px;
            font-size: 15px;
            padding:10px;
        }
    </style>
</head>
<body>
    <div class="showblog">
        <h1><%=postuser.heading%></h1>
        <p><%=postuser.content%></p>
        <form method="post" action="/delete/<%=postuser.id%>?_method=DELETE">
            <button class="delbtn">Confirm Delete</button>
        </form>
</div>
</body>
</html>